// HomepageFeatures is no longer used — features are rendered inline in index.tsx.
// This file is kept as a placeholder to avoid breaking any residual imports.
import type {ReactNode} from 'react';

export default function HomepageFeatures(): ReactNode {
  return null;
}
